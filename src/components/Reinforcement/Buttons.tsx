import { Button } from 'antd';
import React from 'react';

export const Buttons = () => {

    return (
        <div >
          <Button>PDF</Button>
            <Button> Ir al siguiente</Button>
        </div>
    );

};
